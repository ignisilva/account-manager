export interface Account {
  profile: string;
  title: string;
  accountNum: string;
  balance: number;
}

export interface SavingBox {
  title: string;
  saving_goal: number;
  saving_now: number;
}
